# 源程序交付说明

本目录对应老师要求的“源程序”。

## 当前包含内容

- `pascal-s-compiler-src.zip`

该压缩包由仓库当前源码版本导出，主要用于提交“干净源码”。其中包含：

- `src/`：编译器核心源码
- `course_tests/`：课程自建测试脚本与样例
- `user_tests/`：本地回归测试样例
- `CMakeLists.txt`、`Makefile`：构建入口
- 其余必要的仓库内源码与说明文件

## 交付建议

若老师允许仓库链接，可同时提供：

1. Git 仓库地址
2. 本目录中的源码压缩包

若只接受文件提交，则直接使用本目录中的 `pascal-s-compiler-src.zip` 即可。

## 交付前核对项

- [ ] 压缩包可正常解压
- [ ] 不包含 `build-*` 等构建目录
- [ ] 不包含截图缓存、临时文件和无关大文件
- [ ] 解压后可根据 `CMakeLists.txt` / `Makefile` 重建程序
