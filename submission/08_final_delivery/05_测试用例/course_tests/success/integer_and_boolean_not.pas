program IntegerAndBooleanNot(input, output);
var
    a : integer;
    flag : boolean;
begin
    a := 60;
    flag := false;
    write(not a);
    if not flag then
        write(1)
    else
        write(0);
end.
